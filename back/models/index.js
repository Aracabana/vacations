const User = require('./user.model');
const Vacation = require('./vacation.model');
const ActiveWidgets = require('./activeWidgets.model');
const BudgetWidget = require('./budget.widget.model');
const Countries = require('./country.model');

module.exports = { User, Vacation, ActiveWidgets, BudgetWidget, Countries };
